# Leave unset (default) to run the fully client-side demo -- no backend
# needed, what the standalone Vercel deploy uses.
# Set this to point the UI at the real Python backend instead:
# VITE_API_BASE=http://localhost:8000
